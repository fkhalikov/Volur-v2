# EODHD API Token (required)
EODHD_API_TOKEN=your_api_token_here

# SQL Server configuration (Docker compose provides defaults)
# SA password used by the SQL Server container and API connection string
# Keep this strong in real deployments
SA_PASSWORD=YourStrong@Passw0rd
# Optionally override API connection string
# SQLSERVER_CONNECTION_STRING=Server=sqlserver;Database=Volur;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True;

# Cache TTL Configuration (optional)
# CACHE_TTL_EXCHANGES_HOURS=24
# CACHE_TTL_SYMBOLS_HOURS=24

